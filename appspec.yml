version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
hooks:
  BeforeInstall:
    - location: forecast_trigger/scripts/before-install.sh
      timeout: 3600
      runas: root
  ApplicationStart:
    - location: forecast_trigger/scripts/install.sh
      timeout: 3600
      runas: root
